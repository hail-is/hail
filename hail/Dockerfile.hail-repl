FROM {{ base_image.image }}
ENV PYTHONPATH ""
RUN apt-fast update && \
  apt-fast -y install \
    liblz4-dev
COPY wheel-container.tar ./
RUN tar -xf wheel-container.tar && \
    python3 -m hailpip -U hail-*-py3-none-any.whl && \
    rm -rf hail-*-py3-none-any.whl
