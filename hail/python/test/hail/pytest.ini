[pytest]
asyncio_mode = auto
addopts = --strict-markers
markers =
    unchecked_allocator: tests that use the unchecked allocator
    asyncio: test files that use asyncio
    run_in: specifies where to run the tests
filterwarnings =
    error
    ignore::UserWarning
    ignore::DeprecationWarning
xfail_strict=true
