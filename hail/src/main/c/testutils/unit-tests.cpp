#define CATCH_CONFIG_MAIN
#include "catch.hpp"

TEST_CASE("this test case runs successfully") {
  // this is being used to test the unit testing stuff and will be removed
  // in the subsequent PR when actual tests are added.
  REQUIRE(true);
}