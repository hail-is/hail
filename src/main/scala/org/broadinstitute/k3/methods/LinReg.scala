package org.broadinstitute.k3.methods

import breeze.linalg._

import org.broadinstitute.k3.variant._

object LinReg {
  def name = "LinearRegression"

  //def apply(vds: VariantDataset, cov: DenseMatrix): Map[Variant, Double] = {
  //  require(vds.sampleIds.length == cov.rows)

  def apply: String = "Hi"
}
