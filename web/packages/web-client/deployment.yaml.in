apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment
  labels:
    app: web
    hail.is/sha: "@sha@"
spec:
  selector:
    matchLabels:
      app: web
  replicas: 1
  template:
    metadata:
      labels:
        app: web
        hail.is/sha: "@sha@"
    spec:
      serviceAccountName: hail-web
      containers:
        - name: web
          image: @image@
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef: 
                name: @secrets@
          command: ["npm", "run", "deploy"]
---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
    hail.is/sha: "@sha@"
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: web
